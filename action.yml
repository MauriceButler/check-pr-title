name: 'Check PR Title'
description: 'Check a pull request title matches a given regex'
inputs:
    regex: # id of input
        description: 'Regex for PR title to match'
        required: true
runs:
    using: 'node16'
    main: 'dist/index.js'
